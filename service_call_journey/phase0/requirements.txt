# Phase 0 is standard-library Python 3.10+. These two are only needed as noted.
openpyxl>=3.1        # ExportInvoice_*.xlsx importer (already installed for the sales-side importer)
pyodbc>=5.0          # SQL Server in production only; tests and dev use the built-in sqlite3
